{
  "name": "ktx2-vram-measurement",
  "version": "1.0.0",
  "description": "Doku belleğini byte-per-pixel formülüyle ölçmek: blok hizalaması, mip zinciri, cubemap çarpanı, KTX2 transcode hedefi ve formülü sürücüye compressedTexImage2D ile doğrulatan bir tarayıcı probu",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "predev": "npm run prepare-basis",
    "build": "tsc && vite build",
    "prebuild": "npm run prepare-basis",
    "preview": "vite preview",
    "prepare-basis": "node scripts/copy-basis.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/three": "0.185.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
